import { Toaster } from "@/components/ui/sonner";
import type { Metadata } from "next";
import {
  ClerkProvider,
  Show,
  SignInButton,
  SignUpButton,
  UserButton,
} from "@clerk/nextjs";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Chat With Pdf",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.className} antialiased min-h-screen h-screen overflow-hidden flex flex-col`}
      >
        <Toaster />
        <ClerkProvider>
          {/* <header className="flex justify-end items-center p-4 gap-4 h-16">
            <Show when="signed-out">
              <SignInButton>
                <button className=' text-black rounded-full font-medium text-sm h-12 px-5 cursor-pointer hover:scale-103 transition-transform duration-300 ease-in-out'>Sign In</button>
              </SignInButton>
              <SignUpButton>
                <button className="bg-purple-700 text-white rounded-full font-medium text-sm h-12 px-5 cursor-pointer hover:scale-103 transition-transform duration-300 ease-in-out">
                  Sign Up
                </button>
              </SignUpButton>
            </Show>
            <Show when="signed-in">
              <UserButton />
            </Show>
          </header> */}
          {children}
        </ClerkProvider>
      </body>
    </html>
  );
}
